import styled from "styled-components";

const SEditTaskButton = styled.button`
    width: 22px;
    height: 22px;
    background: url("/images/edit.svg") center/cover no-repeat;
`

export default SEditTaskButton;